//============================================================================
// Name        : testLogger.cpp
// Author      : pola17
//============================================================================
/*
#include "../include/Logger.hh"

int main() {

	LOGGER_INIT(Logger::DEBUG);
    LOGGER_DEBUG("en debug");
    LOGGER_INFO("en info");
    LOGGER_ERROR("en error");
    LOGGER_KILL();

    LOGGER_INIT(Logger::INFO);
    LOGGER_DEBUG("en debug");
    LOGGER_INFO("en info");
    LOGGER_ERROR("en error");
    LOGGER_KILL();

    LOGGER_INIT(Logger::ERROR);
    LOGGER_DEBUG("en debug");
    LOGGER_INFO("en info");
    LOGGER_ERROR("en error");
    LOGGER_KILL();

    LOGGER_INIT(Logger::OFF);
    LOGGER_DEBUG("en debug");
    LOGGER_INFO("en info");
    LOGGER_ERROR("en error");
    LOGGER_KILL();

   // FILELESS TESTS
    LOGGER_INIT_FILELESS(Logger::DEBUG);
    LOGGER_DEBUG("en debug");
    LOGGER_INFO("en info");
    LOGGER_ERROR("en error");
    LOGGER_KILL();

    LOGGER_INIT_FILELESS(Logger::INFO);
    LOGGER_DEBUG("en debug");
    LOGGER_INFO("en info");
    LOGGER_ERROR("en error");
    LOGGER_KILL();

    LOGGER_INIT_FILELESS(Logger::ERROR);
    LOGGER_DEBUG("en debug");
    LOGGER_INFO("en info");
    LOGGER_ERROR("en error");
    LOGGER_KILL();

    LOGGER_INIT_FILELESS(Logger::OFF);
    LOGGER_DEBUG("en debug");
    LOGGER_INFO("en info");
    LOGGER_ERROR("en error");
    LOGGER_KILL();
    return 0;
}*/
